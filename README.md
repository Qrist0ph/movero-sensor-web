# movero-sensor-web

Öffentliche Consumer-Landingpage für **movero-sensor** (`https://movero-sensor.de`),
gehostet über **GitHub Pages** (Custom Domain via `CNAME`).

- `index.html` — self-contained (inline CSS), Brand-Assets im Repo-Root.
- Rechtsseiten (`/datenschutz`, `/konto-loeschen`) liegen bewusst NICHT hier, sondern
  am Backend unter `https://app.movero-sensor.de/...` (eine Quelle; Footer verlinkt dorthin).
- App/Backend: siehe Skill `yaico/kundenprojekte/eigene/omaalarm`.
